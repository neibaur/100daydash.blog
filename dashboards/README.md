# Dashboards

Daily dashboard workspaces live here. Each dashboard should include a README,
source code, tests, data folders, and output folders.

Create a new dashboard with:

```bash
.venv\Scripts\uv.exe run python scripts/new-dashboard.py --day 1 --title "US EV Sales Trend"
```
