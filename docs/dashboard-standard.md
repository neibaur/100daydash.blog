# Dashboard Standard

Each dashboard should include a `README.md`, source code, tests, data folders,
and output folders. Daily posts live in `web/src/content/blog/` and include
frontmatter that links the post to a dashboard folder through `dashboardSlug`.

Allowed statuses are `draft`, `published`, and `archived`.
