# Security

The default security baseline uses CodeQL, Gitleaks, Bandit, detect-secrets, and
GitHub dependency review.

Never commit API keys, tokens, passwords, private certificates, browser cookies,
private downloaded data, or production `.env` files.
